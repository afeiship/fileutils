import * as fu from '../src';

describe('api.basic', () => {
  test('normail single value case', () => {
    // console.log(fu.cp_r(''));
    // fu.cp_r('src', '.tmp');
    console.log(Object.keys(fu));

  });
});
